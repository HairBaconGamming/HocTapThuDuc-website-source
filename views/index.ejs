<%- include('partials/header') %>

<style>
    .hero-section {
        text-align: center;
        padding: 80px 20px 40px;
    }
    .hero-badge {
        background: var(--yellow);
        color: #d63384;
        padding: 5px 15px;
        border-radius: 20px;
        font-weight: 700;
        font-size: 0.9rem;
        display: inline-block;
        margin-bottom: 15px;
        box-shadow: 2px 2px 0px rgba(0,0,0,0.05);
    }
    .hero-title {
        font-size: 3rem;
        color: var(--text-main);
        line-height: 1.2;
        margin-bottom: 20px;
    }
    .hero-title span { color: var(--primary); }
    .hero-desc {
        font-size: 1.1rem;
        color: var(--text-light);
        max-width: 600px;
        margin: 0 auto 40px;
    }
    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 30px;
        padding: 40px 0;
    }
    .feature-card {
        text-align: center;
        background: white;
        border-radius: var(--radius);
        padding: 30px;
        border: 2px solid #eee;
        transition: 0.3s;
    }
    .feature-card:hover {
        border-color: var(--secondary);
        transform: translateY(-10px);
    }
    .feature-icon {
        font-size: 3rem;
        margin-bottom: 15px;
        display: block;
    }
</style>

<div class="container">
    <section class="hero-section">
        <% if (!user) { %>
            <div class="hero-badge">🚀 THPT Thủ Đức</div>
            <h1 class="hero-title">Học tập chill chill <br> <span>Kiến thức siêu iu</span></h1>
            <p class="hero-desc">
                Cộng đồng học tập dành riêng cho dân Thủ Đức. <br>
                Nơi chia sẻ tài liệu, thi đua top và giải trí sau giờ học căng thẳng.
            </p>
            <div style="display: flex; gap: 15px; justify-content: center;">
                <a href="/register" class="btn btn-primary" style="padding: 12px 30px; font-size: 1.1rem;">
                    ✨ Nhập hộ khẩu ngay
                </a>
                <a href="/login" class="btn btn-secondary">
                    👋 Đã có nick rồi
                </a>
            </div>
        <% } else { %>
            <div class="hero-badge">👋 Hello, <%= user.username %>!</div>
            <h1 class="hero-title">Hôm nay bạn muốn <br> <span>"xử đẹp"</span> môn nào?</h1>
            
            <div class="dashboard-preview" style="display: flex; gap: 15px; justify-content: center; margin-top: 30px;">
                <div class="stat-bubble" style="background: #e3f2fd; padding: 15px 25px; border-radius: 15px;">
                    <i class="fas fa-star" style="color: #fbc02d;"></i> <b><%= typeof totalVisits !== 'undefined' ? totalVisits : 0 %></b> lượt truy cập
                </div>
                <div class="stat-bubble" style="background: #fce4ec; padding: 15px 25px; border-radius: 15px;">
                    <i class="fas fa-user-graduate" style="color: #d81b60;"></i> <b><%= typeof totalUsers !== 'undefined' ? totalUsers : 0 %></b> thành viên
                </div>
            </div>
        <% } %>
    </section>

    <section class="features-grid">
        <div class="feature-card">
            <span class="feature-icon">📚</span>
            <h3>Tài liệu Chất</h3>
            <p>Tổng hợp đề thi, bài giảng xịn sò từ các trường top.</p>
        </div>
        <div class="feature-card">
            <span class="feature-icon">🌵</span>
            <h3>Nuôi Cây</h3>
            <p>Học chăm chỉ để tích điểm nuôi cây ảo, đổi quà thật.</p>
        </div>
        <div class="feature-card">
            <span class="feature-icon">💬</span>
            <h3>Hỏi đáp 24/7</h3>
            <p>Bí bài thì hỏi, cộng đồng vào cứu net ngay lập tức.</p>
        </div>
    </section>
    
    <% if (latestNews && latestNews.length > 0) { %>
        <section style="margin-top: 40px;">
            <h3 class="text-center mb-2">📰 Bảng Tin Mới Nhất</h3>
            <div class="features-grid">
                <% latestNews.forEach(news => { %>
                    <div class="card news-mini-card">
                        <span class="badge" style="background: #eee; margin-bottom: 10px; display: inline-block;"><%= news.category %></span>
                        <h4><%= news.title %></h4>
                        <a href="/news/<%= news._id %>" class="btn-link" style="color: var(--primary); font-weight: bold; margin-top: 10px; display: block;">Xem chi tiết &rarr;</a>
                    </div>
                <% }) %>
            </div>
        </section>
    <% } %>
</div>

<%- include('partials/footer') %>