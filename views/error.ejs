<%- include('partials/header') %>
<link rel="stylesheet" href="/css/styleError.css">

<div class="error-page-wrapper">
    <div class="error-card">
        <div class="error-icon-visual">🤖</div>
        
        <h1 class="error-code color-500">500</h1>
        <h2 class="error-title">Server đang bị "cảm cúm"</h2>
        
        <p class="error-desc">
            Hệ thống đang gặp chút trục trặc kỹ thuật. Đội ngũ kỹ sư (Admin) đang tích cực fix bug rồi. 
            Bạn thử tải lại trang xem sao nhé?
        </p>

        <div class="btn-group">
            <a href="/" class="btn-err btn-back">
                🏠 Về trang chủ
            </a>
            <button onclick="location.reload()" class="btn-err btn-home" style="background: var(--err-accent-500); color: #333; border-color: #333;">
                🔄 Thử lại (F5)
            </button>
        </div>

        <% if (typeof error !== 'undefined' && error.stack && process.env.NODE_ENV === 'development') { %>
            <div style="margin-top: 30px; text-align: left;">
                <b style="color: #c62828;">Debug Info (Dev Only):</b>
                <div class="stack-trace">
                    <%= error.stack %>
                </div>
            </div>
        <% } %>
    </div>
</div>

<%- include('partials/footer') %>