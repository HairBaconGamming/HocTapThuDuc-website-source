<%- include('partials/header') %>
<link rel="stylesheet" href="/css/styleProfile.css">

<div class="profile-wrapper">
    
    <div class="profile-grid">
        
        <aside class="bento-card identity-card" data-animate="fade-right">
            <div class="id-avatar-wrapper">
                <img src="<%= user.avatar || 'https://cdn-icons-png.flaticon.com/512/847/847969.png' %>" class="id-avatar">
                <% if(user.isPro) { %>
                    <div class="pro-badge" title="Tài khoản VIP">👑</div>
                <% } %>
            </div>

            <h1 class="id-name"><%= user.username %></h1>
            <p class="id-username">@<%= user.username.toLowerCase().replace(/\s+/g, '') %></p>

            <div class="id-meta">
                <span class="meta-tag"><i class="fas fa-graduation-cap"></i> <%= user.class || 'Lớp ?' %></span>
                <span class="meta-tag"><i class="fas fa-school"></i> <%= user.school || 'Trường ?' %></span>
            </div>

            <div class="id-bio">
                "<%= user.bio || 'Chưa có tiểu sử. Hãy viết gì đó thật ngầu!' %>"
            </div>

            <a href="/profile/edit" class="btn-edit-profile">
                <i class="fas fa-pen"></i> Chỉnh sửa hồ sơ
            </a>
            
            <div style="margin-top: 15px;">
                <a href="/logout" style="color: #ff6b6b; font-weight: 700; font-size: 0.9rem;">
                    <i class="fas fa-sign-out-alt"></i> Đăng xuất
                </a>
            </div>
        </aside>

        <main>
            <div class="stats-row" data-animate="fade-down">
                <div class="stat-box highlight">
                    <h3><%= user.points || 0 %></h3>
                    <p>🔥 Điểm số</p>
                </div>
                <div class="stat-box">
                    <h3>12</h3> <p>📚 Bài học</p>
                </div>
                <div class="stat-box">
                    <h3>5</h3> <p>🏆 Top 1</p>
                </div>
            </div>

            <div class="bento-card level-container" data-animate="fade-up">
                <div class="level-header">
                    <span>Level <%= Math.floor((user.points || 0) / 100) + 1 %>: Học Giả Tập Sự</span>
                    <span><%= (user.points || 0) % 100 %>/100 XP</span>
                </div>
                <div class="progress-track">
                    <div class="progress-fill" style="width: <%= (user.points || 0) % 100 %>%;"></div>
                </div>
                <p style="margin-top: 10px; font-size: 0.85rem; color: #666;">
                    Cố lên! <b><%= 100 - ((user.points || 0) % 100) %> điểm</b> nữa để lên cấp tiếp theo.
                </p>
            </div>

            <div class="bento-card tree-section" data-animate="fade-left">
                <div class="tree-info">
                    <h3>🌳 Vườn Ươm Của Tôi</h3>
                    <p style="color: #666; margin-bottom: 15px;">Cây của bạn lớn lên nhờ kiến thức bạn nạp vào.</p>
                    <a href="/my-tree" style="color: var(--primary); font-weight: 800; text-decoration: none;">Thăm vườn &rarr;</a>
                </div>
                <div class="tree-visual">
                    <% 
                        let treeImg = 'seed.png';
                        if((user.points || 0) > 500) treeImg = 'tree-big.png';
                        else if((user.points || 0) > 100) treeImg = 'sprout.png';
                    %>
                    <img src="https://cdn-icons-png.flaticon.com/512/2924/2924900.png" alt="Tree Stage"> 
                </div>
            </div>

            <div class="bento-card" data-animate="fade-up">
                <h3 style="font-size: 1.2rem; margin-bottom: 20px;">📜 Hoạt động gần đây</h3>
                <ul class="activity-list">
                    <li class="activity-item">
                        <div class="activity-icon"><i class="fas fa-check"></i></div>
                        <div class="activity-content">
                            <h4>Hoàn thành bài "Đạo hàm cơ bản"</h4>
                            <span class="activity-time">2 giờ trước</span>
                        </div>
                    </li>
                    <li class="activity-item">
                        <div class="activity-icon" style="background:#e3f2fd; color:#1565c0;"><i class="fas fa-comment"></i></div>
                        <div class="activity-content">
                            <h4>Bình luận tại "Vật lý hạt nhân"</h4>
                            <span class="activity-time">1 ngày trước</span>
                        </div>
                    </li>
                </ul>
            </div>

        </main>
    </div>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
<script>
    if(typeof gsap !== 'undefined') {
        //gsap.from('.identity-card', { x: -50, opacity: 0, duration: 0.8, ease: 'power2.out' });
        gsap.from('.stat-box', { y: 30, opacity: 0, duration: 0.6, stagger: 0.1, delay: 0.2 });
        gsap.from('.bento-card', { y: 30, opacity: 0, duration: 0.6, stagger: 0.2, delay: 0.4 });
    }
</script>

<%- include('partials/footer') %>