<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - Học Tập Thủ Đức</title>
    <link rel="icon" type="image/png" href="https://i.ibb.co/QBLckWj/Gemini-Generated-Image-wt3dr4wt3dr4wt3d-removebg-preview.png">
    
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <style>
        :root {
            --primary: #4f46e5;      /* Indigo */
            --primary-dark: #4338ca;
            --secondary: #06b6d4;    /* Cyan */
            --success: #10b981;      /* Emerald */
            --warning: #f59e0b;      /* Amber */
            --danger: #ef4444;       /* Red */
            --dark: #1e293b;         /* Slate 800 */
            --light: #f1f5f9;        /* Slate 100 */
            --white: #ffffff;
            --text-sub: #64748b;
            --sidebar-width: 260px;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Quicksand', sans-serif; }
        body { background: var(--light); display: flex; height: 100vh; overflow: hidden; color: var(--dark); }

        /* --- SIDEBAR --- */
        .sidebar {
            width: var(--sidebar-width);
            background: #0f172a; /* Slate 900 */
            color: white;
            display: flex;
            flex-direction: column;
            transition: 0.3s;
            box-shadow: 4px 0 20px rgba(0,0,0,0.1);
            z-index: 100;
        }
        .brand {
            padding: 25px;
            font-size: 1.4rem;
            font-weight: 800;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            display: flex;
            align-items: center;
            gap: 12px;
            letter-spacing: -0.5px;
            border-bottom: 1px solid rgba(255,255,255,0.05);
        }
        .menu { flex: 1; padding: 20px 12px; overflow-y: auto; }
        .menu::-webkit-scrollbar { width: 5px; }
        .menu::-webkit-scrollbar-thumb { background: #334155; border-radius: 4px; }
        
        .menu-label { 
            font-size: 0.7rem; text-transform: uppercase; color: #64748b; 
            margin: 20px 15px 8px; font-weight: 700; letter-spacing: 1px;
        }
        .nav-item {
            padding: 12px 20px;
            margin-bottom: 6px;
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.25s ease;
            color: #94a3b8;
            display: flex;
            align-items: center;
            gap: 14px;
            font-weight: 600;
            font-size: 0.95rem;
        }
        .nav-item:hover { color: white; background: rgba(255,255,255,0.05); }
        .nav-item.active {
            background: var(--primary);
            color: white;
            box-shadow: 0 4px 15px rgba(79, 70, 229, 0.4);
        }
        .nav-item i { width: 20px; text-align: center; }

        /* --- MAIN CONTENT --- */
        .main {
            flex: 1;
            padding: 30px 40px;
            overflow-y: auto;
            position: relative;
        }

        /* Header Bar */
        .header-bar {
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
            margin-bottom: 35px;
        }
        .page-title h2 { font-size: 2rem; color: var(--dark); font-weight: 800; letter-spacing: -1px; margin-bottom: 5px; }
        .page-title p { color: var(--text-sub); font-size: 0.95rem; }
        
        .admin-profile { 
            display: flex; align-items: center; gap: 15px; 
            background: var(--white); padding: 8px 15px 8px 20px; 
            border-radius: 50px; box-shadow: 0 4px 6px -1px rgba(0,0,0,0.05);
        }
        .admin-avatar { width: 42px; height: 42px; border-radius: 50%; object-fit: cover; border: 2px solid var(--primary); }

        /* --- STAT CARDS --- */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
            gap: 25px;
            margin-bottom: 30px;
        }
        .stat-card {
            background: var(--white);
            padding: 25px;
            border-radius: 20px;
            box-shadow: 0 4px 20px -5px rgba(0,0,0,0.05);
            display: flex;
            align-items: center;
            justify-content: space-between;
            transition: transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            border: 1px solid #f8fafc;
        }
        .stat-card:hover { transform: translateY(-5px); box-shadow: 0 10px 25px -5px rgba(0,0,0,0.1); }
        .stat-info h3 { font-size: 2.2rem; color: var(--dark); font-weight: 800; line-height: 1.2; }
        .stat-info p { color: var(--text-sub); font-size: 0.9rem; font-weight: 600; }
        .stat-icon {
            width: 55px; height: 55px;
            border-radius: 16px;
            display: flex; align-items: center; justify-content: center;
            font-size: 1.6rem;
        }

        /* --- CHARTS GRID LAYOUT --- */
        .charts-wrapper {
            display: grid;
            grid-template-columns: 2fr 1fr; /* 2 phần trái, 1 phần phải */
            grid-template-rows: auto auto;
            gap: 25px;
            margin-bottom: 30px;
        }

        .chart-card {
            background: var(--white);
            padding: 25px;
            border-radius: 20px;
            box-shadow: 0 4px 20px -5px rgba(0,0,0,0.05);
            border: 1px solid #f8fafc;
            display: flex;
            flex-direction: column;
        }

        .chart-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }
        .chart-header h3 { font-size: 1.1rem; font-weight: 700; color: var(--dark); }

        /* Chart 1: Growth (Full width on mobile, top large on desktop) */
        .chart-growth {
            grid-column: 1 / -1; /* Chiếm hết hàng ngang */
            height: 400px;
        }

        /* Chart 2 & 3: Half width */
        .chart-half {
            height: 350px;
        }

        @media (max-width: 1200px) {
            .charts-wrapper { grid-template-columns: 1fr; }
            .chart-growth { grid-column: 1; }
        }

        /* --- TABLES --- */
        .table-wrapper {
            background: var(--white);
            padding: 30px;
            border-radius: 24px;
            box-shadow: 0 4px 20px -5px rgba(0,0,0,0.05);
            animation: fadeIn 0.4s ease;
        }
        .table-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 25px; }
        .table-header h3 { font-size: 1.25rem; font-weight: 700; color: var(--dark); }
        
        .search-box { position: relative; }
        .search-box input {
            padding: 12px 20px 12px 45px;
            border: 2px solid #e2e8f0;
            border-radius: 12px;
            width: 300px;
            outline: none;
            transition: 0.2s;
            font-weight: 600;
            color: var(--dark);
        }
        .search-box i { position: absolute; left: 15px; top: 50%; transform: translateY(-50%); color: #94a3b8; }
        .search-box input:focus { border-color: var(--primary); box-shadow: 0 0 0 4px rgba(79, 70, 229, 0.1); }
        
        table { width: 100%; border-collapse: separate; border-spacing: 0; }
        th { 
            text-align: left; padding: 15px 20px; 
            color: #64748b; font-weight: 700; font-size: 0.8rem; 
            text-transform: uppercase; letter-spacing: 0.5px;
            border-bottom: 2px solid #f1f5f9;
        }
        td { padding: 16px 20px; border-bottom: 1px solid #f1f5f9; color: var(--dark); vertical-align: middle; }
        tr:last-child td { border-bottom: none; }
        tr:hover td { background: #f8fafc; }
        
        .badge { padding: 6px 12px; border-radius: 30px; font-size: 0.75rem; font-weight: 700; display: inline-block; }
        .badge-pro { background: #fff7ed; color: #ea580c; border: 1px solid #fed7aa; }
        .badge-teacher { background: #eff6ff; color: #2563eb; border: 1px solid #bfdbfe; }
        .badge-admin { background: #fef2f2; color: #dc2626; border: 1px solid #fecaca; }
        .badge-user { background: #f1f5f9; color: #475569; }

        /* BUTTONS */
        .btn { 
            padding: 10px 20px; border-radius: 10px; border: none; cursor: pointer; 
            font-weight: 600; font-size: 0.9rem; transition: 0.2s; color: white; 
            display: inline-flex; align-items: center; gap: 8px; text-decoration: none;
        }
        .btn:active { transform: scale(0.95); }
        .btn-primary { background: var(--primary); box-shadow: 0 4px 10px rgba(79, 70, 229, 0.25); }
        .btn-primary:hover { background: var(--primary-dark); transform: translateY(-2px); }
        .btn-danger { background: var(--danger); box-shadow: 0 4px 10px rgba(239, 68, 68, 0.25); }
        .btn-warning { background: var(--warning); color: white; }
        .btn-sm { padding: 8px 12px; font-size: 0.85rem; border-radius: 8px; }
        .action-group { display: flex; gap: 8px; }

        /* MODALS & FORMS */
        .section { display: none; }
        .section.active { display: block; animation: fadeIn 0.3s ease; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

        .modal { display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(15, 23, 42, 0.6); backdrop-filter: blur(4px); z-index: 1000; justify-content: center; align-items: center; }
        .modal-content { 
            background: white; padding: 40px; border-radius: 24px; width: 500px; max-width: 95%; 
            box-shadow: 0 25px 50px -12px rgba(0,0,0,0.25); animation: slideUp 0.3s cubic-bezier(0.16, 1, 0.3, 1); 
        }
        @keyframes slideUp { from { transform: translateY(50px) scale(0.9); opacity: 0; } to { transform: translateY(0) scale(1); opacity: 1; } }
        
        input, textarea, select {
            width: 100%; padding: 14px; margin: 8px 0 20px; border: 2px solid #e2e8f0; 
            border-radius: 12px; font-size: 0.95rem; outline: none; transition: 0.2s;
        }
        input:focus, textarea:focus, select:focus { border-color: var(--primary); background: #fff; }
        label { font-weight: 700; color: #475569; font-size: 0.9rem; margin-bottom: 5px; display: block; }
        
        .key-input-group { display: flex; gap: 10px; align-items: center; margin-bottom: 20px; }
        .key-input-group input { margin-bottom: 0; flex: 1; font-family: monospace; letter-spacing: 1px; color: var(--primary); font-weight: bold; background: #f8fafc; }
    </style>
</head>
<body>

    <div class="sidebar">
        <div class="brand">
            <i class="fas fa-rocket"></i> Admin Panel
        </div>
        
        <div class="menu">
            <div class="menu-label">Tổng Quan</div>
            <div class="nav-item active" onclick="showTab('dashboard')">
                <i class="fas fa-chart-pie"></i> Dashboard
            </div>

            <div class="menu-label">Quản Lý</div>
            <div class="nav-item" onclick="showTab('users')">
                <i class="fas fa-users"></i> Người Dùng
            </div>
            <div class="nav-item" onclick="showTab('courses')">
                <i class="fas fa-layer-group"></i> Khóa Học
            </div>
            <div class="nav-item" onclick="showTab('subjects')">
                <i class="fas fa-book"></i> Môn Học
            </div>
            <div class="nav-item" onclick="showTab('news')">
                <i class="fas fa-newspaper"></i> Tin Tức
            </div>

            <div class="menu-label">Hệ Thống</div>
            <a href="/" class="nav-item" style="text-decoration: none;">
                <i class="fas fa-home"></i> Trang Chủ
            </a>
            <a href="/logout" class="nav-item" style="text-decoration: none; color: #fca5a5;">
                <i class="fas fa-sign-out-alt"></i> Đăng Xuất
            </a>
        </div>
    </div>

    <div class="main">
        <div class="header-bar">
            <div class="page-title">
                <h2 id="pageHeaderTitle">Dashboard</h2>
                <p>Hôm nay là <%= new Date().toLocaleDateString('vi-VN', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %></p>
            </div>
            <div class="admin-profile">
                <div style="text-align: right; line-height: 1.2;">
                    <div style="font-weight: 700; color: var(--dark); font-size: 0.9rem;"><%= user.username %></div>
                    <div style="font-size: 0.75rem; color: var(--success);">Online</div>
                </div>
                <img src="<%= user.avatar || 'https://cdn-icons-png.flaticon.com/512/847/847969.png' %>" class="admin-avatar">
            </div>
        </div>

        <div id="dashboard" class="section active">
            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-info">
                        <h3><%= stats.totalUsers %></h3>
                        <p>Tổng Thành Viên</p>
                    </div>
                    <div class="stat-icon" style="background: #e0e7ff; color: var(--primary);">
                        <i class="fas fa-users"></i>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-info">
                        <h3><%= stats.totalCourses %></h3>
                        <p>Khóa Học</p>
                    </div>
                    <div class="stat-icon" style="background: #ecfeff; color: var(--secondary);">
                        <i class="fas fa-book"></i>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-info">
                        <h3><%= stats.totalNews %></h3>
                        <p>Bài Viết</p>
                    </div>
                    <div class="stat-icon" style="background: #fef9c3; color: var(--warning);">
                        <i class="far fa-newspaper"></i>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-info">
                        <h3><%= stats.totalLessons %></h3>
                        <p>Bài Giảng</p>
                    </div>
                    <div class="stat-icon" style="background: #fee2e2; color: var(--danger);">
                        <i class="fas fa-play-circle"></i>
                    </div>
                </div>
            </div>

            <div class="charts-wrapper">
                
                <div class="chart-card chart-growth">
                    <div class="chart-header">
                        <h3>📈 Tăng trưởng hệ thống (7 ngày qua)</h3>
                        <div style="font-size: 0.8rem; color: #64748b; background: #f1f5f9; padding: 5px 10px; border-radius: 8px;">
                            <i class="fas fa-sync-alt"></i> Real-time
                        </div>
                    </div>
                    <div style="flex:1; position: relative;">
                        <canvas id="growthChart"></canvas>
                    </div>
                </div>

                <div class="chart-card chart-half">
                    <div class="chart-header">
                        <h3>👥 Cơ cấu người dùng</h3>
                    </div>
                    <div style="flex:1; position: relative; display: flex; justify-content: center;">
                        <canvas id="userChart"></canvas>
                    </div>
                </div>

                <div class="chart-card chart-half">
                    <div class="chart-header">
                        <h3>📚 Phân bố nội dung</h3>
                    </div>
                    <div style="flex:1; position: relative;">
                        <canvas id="contentChart"></canvas>
                    </div>
                </div>

            </div>
        </div>

        <div id="users" class="section">
            <div class="table-wrapper">
                <div class="table-header">
                    <h3>Danh sách người dùng</h3>
                    <div class="search-box">
                        <i class="fas fa-search"></i>
                        <input type="text" id="userSearch" placeholder="Tìm kiếm user..." onkeyup="filterTable('userTable', this.value)">
                    </div>
                </div>
                <table id="userTable">
                    <thead>
                        <tr>
                            <th>User Info</th>
                            <th>Vai trò</th>
                            <th>Trạng Thái</th>
                            <th>Hành Động</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% users.forEach(u => { %>
                        <tr>
                            <td>
                                <div style="display: flex; align-items: center; gap: 12px;">
                                    <img src="<%= u.avatar || 'https://cdn-icons-png.flaticon.com/512/847/847969.png' %>" style="width: 40px; height: 40px; border-radius: 50%; border: 2px solid #f1f5f9;">
                                    <div>
                                        <div style="font-weight: 700; color: var(--dark); font-size: 0.95rem;"><%= u.username %></div>
                                        <div style="font-size: 0.8rem; color: #94a3b8;"><%= u.email %></div>
                                    </div>
                                </div>
                            </td>
                            <td>
                                <% if (u.username === 'truonghoangnam') { %>
                                    <span class="badge" style="background: #0f172a; color: #f8fafc; border: 1px solid #334155;">👑 OWNER</span>
                                <% } else if (u.isAdmin) { %>
                                    <span class="badge badge-admin">Admin</span>
                                <% } else { %>
                                    <span class="badge badge-user">User</span>
                                <% } %>
                            </td>
                            <td>
                                <% if(u.isPro) { %><span class="badge badge-pro">PRO</span><% } %>
                                <% if(u.isTeacher) { %><span class="badge badge-teacher">GV</span><% } %>
                            </td>
                            <td>
                                <div class="action-group">
                                    <% if (u.username !== 'truonghoangnam' || user.username === 'truonghoangnam') { %>
                                        <button class="btn btn-warning btn-sm" 
                                            onclick="openUserModal('<%= u._id %>', '<%= u.username %>', <%= u.isAdmin %>, <%= u.isPro %>, <%= u.isTeacher %>, '<%= u.proSecretKey || '' %>')">
                                            <i class="fas fa-edit"></i>
                                        </button>
                                    <% } %>
                                    <% if (u.username !== 'truonghoangnam') { %>
                                        <button class="btn btn-danger btn-sm" onclick="confirmDelete('/admin/user/delete', 'userId', '<%= u._id %>')">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    <% } %>
                                </div>
                            </td>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>

        <div id="courses" class="section">
            <div class="table-wrapper">
                <div class="table-header">
                    <h3>Quản lý khóa học</h3>
                    <div class="search-box">
                        <i class="fas fa-search"></i>
                        <input type="text" id="courseSearch" placeholder="Tìm khóa học..." onkeyup="filterTable('courseTable', this.value)">
                    </div>
                </div>
                <table id="courseTable">
                    <thead>
                        <tr>
                            <th>Tên Khóa Học</th>
                            <th>Tác Giả</th>
                            <th>Tình Trạng</th>
                            <th>Hành Động</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% courses.forEach(c => { %>
                        <tr>
                            <td style="font-weight: 700;"><%= c.title %></td>
                            <td>
                                <span class="badge badge-teacher"><i class="fas fa-chalkboard-teacher"></i> <%= c.author ? c.author.username : 'Ẩn danh' %></span>
                            </td>
                            <td>
                                <% if(c.isPublished) { %>
                                    <span class="badge" style="background:#dcfce7; color:#166534;"><i class="fas fa-check-circle"></i> Public</span>
                                <% } else { %>
                                    <span class="badge" style="background:#f1f5f9; color:#64748b;"><i class="fas fa-lock"></i> Private</span>
                                <% } %>
                            </td>
                            <td>
                                <div class="action-group">
                                    <form action="/admin/course/approve" method="POST" style="margin:0;">
                                        <input type="hidden" name="courseId" value="<%= c._id %>">
                                        <input type="hidden" name="isPublished" value="<%= c.isPublished ? '' : 'on' %>">
                                        <button class="btn btn-sm <%= c.isPublished ? 'btn-warning' : 'btn-primary' %>" title="<%= c.isPublished ? 'Ẩn' : 'Duyệt' %>">
                                            <i class="fas <%= c.isPublished ? 'fa-eye-slash' : 'fa-check' %>"></i>
                                        </button>
                                    </form>
                                    <button class="btn btn-danger btn-sm" onclick="confirmDelete('/admin/course/delete', 'courseId', '<%= c._id %>')">
                                        <i class="fas fa-trash"></i>
                                    </button>
                                </div>
                            </td>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>

        <div id="subjects" class="section">
            <div class="table-wrapper">
                <div class="table-header">
                    <h3>Quản lý môn học</h3>
                    <div style="display: flex; gap: 10px;">
                        <div class="search-box" style="margin-bottom: 0;">
                            <i class="fas fa-search"></i>
                            <input type="text" id="subjectSearch" placeholder="Tìm môn..." onkeyup="filterTable('subjectTable', this.value)">
                        </div>
                        <button class="btn btn-primary" onclick="openSubjectModal('', '', '')">
                            <i class="fas fa-plus"></i> Thêm Môn
                        </button>
                    </div>
                </div>
                <table id="subjectTable">
                    <thead>
                        <tr>
                            <th>Ảnh</th>
                            <th>Tên Môn</th>
                            <th>Hành Động</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if(typeof subjects !== 'undefined') { %>
                            <% subjects.forEach(s => { %>
                            <tr>
                                <td>
                                    <img src="<%= s.image || 'https://cdn-icons-png.flaticon.com/512/3135/3135715.png' %>" 
                                         style="width: 45px; height: 45px; object-fit: contain; border-radius: 12px; background: #f8fafc; padding: 6px; border: 1px solid #e2e8f0;">
                                </td>
                                <td>
                                    <div style="font-weight: 700; color: var(--dark); font-size: 1rem;"><%= s.name %></div>
                                    <div style="font-size: 0.75rem; color: #94a3b8; font-family: monospace;">ID: <%= s._id %></div>
                                </td>
                                <td>
                                    <div class="action-group">
                                        <button class="btn btn-warning btn-sm" onclick="openSubjectModal('<%= s._id %>', '<%= s.name %>', '<%= s.image %>')">
                                            <i class="fas fa-edit"></i>
                                        </button>
                                        <button class="btn btn-danger btn-sm" onclick="confirmDelete('/admin/subject/delete', 'subjectId', '<%= s._id %>')">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </div>
                                </td>
                            </tr>
                            <% }); %>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>

        <div id="news" class="section">
            <div class="table-wrapper">
                <div class="table-header">
                    <h3>Quản lý tin tức</h3>
                    <button class="btn btn-primary" onclick="document.getElementById('modalNews').style.display='flex'">
                        <i class="fas fa-pen-nib"></i> Viết Bài Mới
                    </button>
                </div>
                <table id="newsTable">
                    <thead>
                        <tr>
                            <th>Tiêu đề</th>
                            <th>Danh mục</th>
                            <th>Ngày đăng</th>
                            <th>Hành động</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% news.forEach(post => { %>
                        <tr>
                            <td style="font-weight: 700; color: var(--dark);"><%= post.title %></td>
                            <td><span class="badge" style="background:#f3e8ff; color:#7e22ce;"><%= post.category %></span></td>
                            <td style="color: #64748b;"><%= new Date(post.createdAt).toLocaleDateString('vi-VN') %></td>
                            <td>
                                <button class="btn btn-danger btn-sm" onclick="confirmDelete('/admin/news/delete', 'newsId', '<%= post._id %>')">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </td>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <div id="modalUser" class="modal">
        <div class="modal-content">
            <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:25px;">
                <h3 style="margin:0; font-size: 1.5rem;">🛠 Chỉnh sửa User</h3>
                <i class="fas fa-times" style="cursor:pointer; font-size:1.4rem; color: #94a3b8;" onclick="document.getElementById('modalUser').style.display='none'"></i>
            </div>
            
            <form action="/admin/user/update" method="POST">
                <input type="hidden" name="userId" id="editUserId">
                <p style="margin-bottom: 20px; font-weight: 600;">Đang sửa: <span id="editUsername" style="color:var(--primary)"></span></p>

                <div style="display: grid; grid-template-columns: 1fr; gap: 15px; margin-bottom: 20px;">
                    <% if (user && user.username === 'truonghoangnam') { %>
                        <label style="background: #fef2f2; padding: 15px; border-radius: 12px; border: 1px solid #fecaca; cursor: pointer; color: #dc2626; display: flex; align-items: center; gap: 10px;">
                            <input type="checkbox" name="isAdmin" id="editIsAdmin" style="width: auto; margin: 0;"> 🛡️ Cấp quyền Admin
                        </label>
                    <% } %>
                    <label style="background: #fffbeb; padding: 15px; border-radius: 12px; border: 1px solid #fcd34d; cursor: pointer; display: flex; align-items: center; gap: 10px;">
                        <input type="checkbox" name="isPro" id="editIsPro" style="width: auto; margin: 0;"> 👑 Tài khoản PRO
                    </label>
                    <label style="background: #eff6ff; padding: 15px; border-radius: 12px; border: 1px solid #bfdbfe; cursor: pointer; display: flex; align-items: center; gap: 10px;">
                        <input type="checkbox" name="isTeacher" id="editIsTeacher" style="width: auto; margin: 0;"> 🎓 Giáo viên
                    </label>
                </div>

                <label>PRO Secret Key:</label>
                <div class="key-input-group">
                    <input type="text" name="proSecretKey" id="editProKey" placeholder="Chưa có mã...">
                    <button type="button" class="btn btn-warning" onclick="generateKey()">
                        <i class="fas fa-magic"></i> Tạo
                    </button>
                </div>

                <button type="submit" class="btn btn-primary" style="width:100%; padding: 14px; font-size: 1rem;">Lưu Thay Đổi</button>
            </form>
        </div>
    </div>

    <div id="modalSubject" class="modal">
        <div class="modal-content" style="width: 450px;">
            <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:20px;">
                <h3 style="margin:0;">📚 Thông tin môn học</h3>
                <i class="fas fa-times" style="cursor:pointer;" onclick="document.getElementById('modalSubject').style.display='none'"></i>
            </div>
            <form action="/admin/subject/save" method="POST">
                <input type="hidden" name="subjectId" id="editSubjectId">
                <label>Tên môn học:</label>
                <input type="text" name="name" id="editSubjectName" required>
                <label>Link Icon (URL):</label>
                <div style="display: flex; gap: 15px; align-items: flex-start;">
                    <input type="text" name="image" id="editSubjectImage" placeholder="https://..." oninput="document.getElementById('previewIcon').src = this.value || 'https://cdn-icons-png.flaticon.com/512/3135/3135715.png'">
                    <img id="previewIcon" src="https://cdn-icons-png.flaticon.com/512/3135/3135715.png" style="width: 50px; height: 50px; object-fit: contain; border: 1px solid #eee; border-radius: 8px; padding: 5px;">
                </div>
                <button type="submit" class="btn btn-primary" style="width:100%; margin-top: 10px;">Lưu Dữ Liệu</button>
            </form>
        </div>
    </div>

    <div id="modalNews" class="modal">
        <div class="modal-content" style="width: 650px;">
            <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:20px;">
                <h3 style="margin:0;">📰 Đăng tin mới</h3>
                <i class="fas fa-times" style="cursor:pointer;" onclick="document.getElementById('modalNews').style.display='none'"></i>
            </div>
            <form action="/admin/news/create" method="POST">
                <label>Tiêu đề:</label>
                <input type="text" name="title" required placeholder="Nhập tiêu đề...">
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                    <div>
                        <label>Danh mục:</label>
                        <select name="category" required>
                            <option value="Thông báo">Thông báo chung</option>
                            <option value="Học tập">Góc học tập</option>
                            <option value="Tuyển sinh">Tuyển sinh</option>
                            <option value="Tài khoản PRO">Khuyến mãi</option>
                        </select>
                    </div>
                    <div>
                        <label>Ảnh bìa (URL):</label>
                        <input type="text" name="image" placeholder="https://...">
                    </div>
                </div>
                <label>Nội dung:</label>
                <textarea name="content" rows="6" required placeholder="Nội dung bài viết..."></textarea>
                <button type="submit" class="btn btn-primary" style="width:100%;">Đăng Bài</button>
            </form>
        </div>
    </div>

    <script>
        const serverChartData = <%- (typeof chartData !== 'undefined' && chartData) ? JSON.stringify(chartData) : '{}' %>;

        function showTab(tabId) {
            document.querySelectorAll('.section').forEach(el => el.classList.remove('active'));
            document.querySelectorAll('.nav-item').forEach(el => el.classList.remove('active'));
            
            const tabSection = document.getElementById(tabId);
            if (tabSection) tabSection.classList.add('active');
            
            const menuMap = {'dashboard': 0, 'users': 1, 'courses': 2, 'subjects': 3, 'news': 4};
            const menus = document.querySelectorAll('.nav-item');
            if (menus[menuMap[tabId]]) menus[menuMap[tabId]].classList.add('active');

            const titles = {
                'dashboard': 'Dashboard', 'users': 'Quản Lý Người Dùng', 
                'courses': 'Quản Lý Khóa Học', 'subjects': 'Quản Lý Môn Học', 'news': 'Quản Lý Tin Tức'
            };
            if(titles[tabId]) document.getElementById('pageHeaderTitle').innerText = titles[tabId];

            const url = new URL(window.location);
            url.searchParams.set('tab', tabId);
            window.history.pushState({}, '', url);
        }

        function filterTable(tableId, query) {
            const filter = query.toUpperCase();
            const rows = document.getElementById(tableId).getElementsByTagName("tr");
            for (let i = 1; i < rows.length; i++) {
                const txt = rows[i].textContent || rows[i].innerText;
                rows[i].style.display = txt.toUpperCase().indexOf(filter) > -1 ? "" : "none";
            }
        }

        // --- RENDER 3 BIỂU ĐỒ ---
        function renderCharts(data) {
            Chart.defaults.font.family = "'Quicksand', sans-serif";
            Chart.defaults.color = '#64748b';

            // 1. GROWTH CHART (LINE - Dual Axis)
            if(document.getElementById('growthChart')) {
                const ctx1 = document.getElementById('growthChart').getContext('2d');
                
                const gradientVisits = ctx1.createLinearGradient(0, 0, 0, 400);
                gradientVisits.addColorStop(0, 'rgba(79, 70, 229, 0.4)');
                gradientVisits.addColorStop(1, 'rgba(79, 70, 229, 0.0)');

                const gradientReg = ctx1.createLinearGradient(0, 0, 0, 400);
                gradientReg.addColorStop(0, 'rgba(16, 185, 129, 0.4)');
                gradientReg.addColorStop(1, 'rgba(16, 185, 129, 0.0)');

                new Chart(ctx1, {
                    type: 'line',
                    data: {
                        labels: data.growth.labels,
                        datasets: [
                            {
                                label: 'Truy cập',
                                data: data.growth.visits,
                                borderColor: '#4f46e5',
                                backgroundColor: gradientVisits,
                                borderWidth: 3,
                                fill: true,
                                tension: 0.4,
                                yAxisID: 'y',
                                pointRadius: 4,
                                pointHoverRadius: 8
                            },
                            {
                                label: 'User mới',
                                data: data.growth.registers,
                                borderColor: '#10b981',
                                backgroundColor: gradientReg,
                                borderWidth: 2,
                                borderDash: [5,5],
                                fill: true,
                                tension: 0.4,
                                yAxisID: 'y1',
                                pointRadius: 4
                            }
                        ]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        interaction: { mode: 'index', intersect: false },
                        plugins: { legend: { position: 'top', align: 'end' } },
                        scales: {
                            x: { grid: { display: false } },
                            y: { display: true, position: 'left', grid: { borderDash: [5, 5] } },
                            y1: { display: true, position: 'right', grid: { display: false }, min: 0 }
                        }
                    }
                });
            }

            // 2. USER CHART (DOUGHNUT)
            if(document.getElementById('userChart')) {
                const ctx2 = document.getElementById('userChart').getContext('2d');
                new Chart(ctx2, {
                    type: 'doughnut',
                    data: {
                        labels: data.userPie.labels,
                        datasets: [{
                            data: data.userPie.data,
                            backgroundColor: [
                                '#cbd5e1', // Thường
                                '#f59e0b', // PRO
                                '#3b82f6', // Teacher
                                '#ef4444'  // Admin
                            ],
                            borderWidth: 0,
                            hoverOffset: 10
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        cutout: '70%',
                        plugins: {
                            legend: { position: 'bottom', labels: { usePointStyle: true, padding: 20 } }
                        }
                    }
                });
            }

            // 3. CONTENT CHART (BAR)
            if(document.getElementById('contentChart')) {
                const ctx3 = document.getElementById('contentChart').getContext('2d');
                new Chart(ctx3, {
                    type: 'bar',
                    data: {
                        labels: data.contentBar.labels,
                        datasets: [{
                            label: 'Số khóa học',
                            data: data.contentBar.data,
                            backgroundColor: '#6366f1',
                            borderRadius: 6,
                            barThickness: 30
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        plugins: { legend: { display: false } },
                        scales: {
                            y: { beginAtZero: true, grid: { borderDash: [5, 5] }, ticks: { precision: 0 } },
                            x: { grid: { display: false } }
                        }
                    }
                });
            }
        }

        window.onload = function() {
            const params = new URLSearchParams(window.location.search);
            const tab = params.get('tab') || 'dashboard';
            showTab(tab);

            if(serverChartData && serverChartData.growth) {
                renderCharts(serverChartData);
            }
        }

        function openUserModal(id, username, isAdmin, isPro, isTeacher, key) {
            document.getElementById('editUserId').value = id;
            document.getElementById('editUsername').innerText = username;
            
            const adminCheck = document.getElementById('editIsAdmin');
            if (adminCheck) adminCheck.checked = isAdmin;
            
            document.getElementById('editIsPro').checked = isPro;
            document.getElementById('editIsTeacher').checked = isTeacher;
            document.getElementById('editProKey').value = key;
            
            document.getElementById('modalUser').style.display = 'flex';
        }

        function openSubjectModal(id, name, image) {
            document.getElementById('editSubjectId').value = id;
            document.getElementById('editSubjectName').value = name;
            document.getElementById('editSubjectImage').value = image;
            document.getElementById('previewIcon').src = image || 'https://cdn-icons-png.flaticon.com/512/3135/3135715.png';
            document.getElementById('modalSubject').style.display = 'flex';
        }

        function generateKey() {
            const charset = "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";
            let retVal = "";
            for (let i = 0; i < 12; ++i) retVal += charset.charAt(Math.floor(Math.random() * charset.length));
            document.getElementById('editProKey').value = "PRO-" + retVal;
        }

        function confirmDelete(actionUrl, fieldName, idValue) {
            Swal.fire({
                title: 'Xác nhận xóa?',
                text: "Dữ liệu sẽ bị xóa vĩnh viễn!",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#ef4444',
                cancelButtonColor: '#64748b',
                confirmButtonText: 'Xóa ngay',
                cancelButtonText: 'Hủy'
            }).then((result) => {
                if (result.isConfirmed) {
                    const form = document.createElement('form');
                    form.method = 'POST';
                    form.action = actionUrl;
                    const input = document.createElement('input');
                    input.type = 'hidden';
                    input.name = fieldName;
                    input.value = idValue;
                    form.appendChild(input);
                    document.body.appendChild(form);
                    form.submit();
                }
            })
        }

        window.onclick = function(e) {
            if (e.target.classList.contains('modal')) e.target.style.display = "none";
        }
    </script>
</body>
</html>