// Custom cursor feature removed — stub to avoid runtime errors if still referenced.
document.addEventListener('DOMContentLoaded', () => {
  // Intentionally empty: custom cursor removed from the project.
  // Keep this file as a harmless stub to avoid 404s or script errors.
  if (typeof console !== 'undefined') console.log('customcursor: disabled');
});